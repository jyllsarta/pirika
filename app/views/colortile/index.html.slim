head
  = csrf_meta_tags
  title
    | しろこのカラータイル
  meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no"
.main
  h1
    | しろこのカラータイル
  .game_screen
    .start
      img.title_image src="images/tile/title.png"
      .namearea
        span.namelabel
          | おなまえ(なまえ#任意の文字列 の形式で入力すると名札📛がつきます)
        div.fixed_username
          span.text_username
            | 　　　　
          span.change_username
            | 変更
        input.username.hidden type="text" name="username" cols="30" rows="1" maxlength=20
      .click_to_start#easy
        .difficulty
          .label
            | かんたん
        .highscore
          img.score_icon src="images/tile/highscore.png"
          span.highscore_value
            | -
        .best_time
          img.score_icon src="images/tile/time.png"
          span.highscore_time
            | -
      .click_to_start#normal
        .difficulty
          .label
            | ふつう
        .highscore
          img.score_icon src="images/tile/highscore.png"
          span.highscore_value
            | -
        .best_time
          img.score_icon src="images/tile/time.png"
          span.highscore_time
            | -
      .click_to_start#hard
        .difficulty
          .label
            | やばみ
        .highscore
          img.score_icon src="images/tile/highscore.png"
          span.highscore_value
            | -
        .best_time
          img.score_icon src="images/tile/time.png"
          span.highscore_time
            | -
    .ui
      .time_base
      .time
      .scorebox
      .score
        | 0
      #effects
    .end.hidden
      .final_score
      .restart.button
        | リスタート
      .back_to_title.button
        | タイトル
    .board
      - (0..@column-1).each do |y|
        .row
          - (0..@row-1).each do |x|
            .panel x=x y=y
              .block.hidden
.colors.removed
  - (1..12).each do |i|
    div[class="color_sample#{i}"]

= stylesheet_link_tag "colortile"
= javascript_include_tag "jquery-3.3.1.min"
= javascript_include_tag "jquery.cookie"
= javascript_include_tag "jquery.easing.1.3"
= javascript_include_tag "jquery-numerator"
= javascript_include_tag "tile/sha256"
= javascript_include_tag "tile/colortile", type: "module"
= javascript_include_tag "tile/view", type: "module"
